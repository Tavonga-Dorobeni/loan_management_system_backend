# Migrations

This directory contains Sequelize CLI migrations for the scaffolded baseline schema.

- Keep schema changes additive and reversible.
- Add real tables and constraints as domain requirements become clear.
